"use client"

import { Doc } from "@/convex/_generated/dataModel"

interface titleProps {
    initialData: Doc <"documents">
}

export const Title = () => {
    return (
        <div>
            title
        </div>
    )
}